```python
import os
import shutil
import time
import uuid
from fastapi.params import Depends, File
from fastapi.responses import FileResponse
import redis
import json
import fastapi

app = fastapi.FastAPI()

from sqlalchemy import create_engine, select
from sqlalchemy.orm import sessionmaker, Mapped, mapped_column, DeclarativeBase, Session
r = redis.Redis(host="localhost", port=6379, db = 0)
from pydantic import BaseModel
class Settings(BaseModel):
 EXPIRED_REDIS:int = 20
settings = Settings()
engine = create_engine("sqlite:///./db.db")
ses = sessionmaker(engine)
class Base(DeclarativeBase):
 ...

def get_ses():
 with ses() as s:
  yield s

class Videos(Base):
 __tablename__="videos"
 id:Mapped[int] = mapped_column(primary_key= True)
 title:Mapped[str]
 urls:Mapped[Photos] = mapped_column(nullable=True)


class Photos(Base):
  url

@app.get("/")
def create_db():
 Base.metadata.create_all(engine)

class VideoSave(BaseModel):
  title:str


@app.post("/video")
def create(video:VideoSave, session:Session = Depends(get_ses)):
  video = Videos(title = video.title)
  session.add(video)
  session.commit()
  session.flush(video)
  return video.id
@app.post("/save_photo")
def save_photo( video_id:int, file:fastapi.UploadFile = File(...),session:Session = Depends(get_ses)):
 print(file.filename, file, file.size)
 video = session.scalar(select(Videos).where(Videos.id == video_id))
 if not video:
   return None


 filename = f"{file.filename}"

 file_path = f"uploads/{filename}"
 video.url = file_path
    # сохраняем файл на fдиск
 with open(file_path, "wb") as buffer:
        shutil.copyfileobj(file.file, buffer)
 video.url = file_path
 session.commit()
 return {"status": "saved", "filename": filename}


@app.get("/d")
def get_video(session:Session = Depends(get_ses)):
 video = session.scalars(select(Videos))
 return video.all()


@app.get("/get/photo/{id}")
def get_photo(id:int,page:int = 10,session:Session = Depends(get_ses)):

  video = session.scalar(select(Videos).where(Videos.id == id))
  
  return FileResponse(video.url)

# <img src="http://localhost:3000/get/photo/1">
```