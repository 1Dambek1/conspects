# 02. Алгебра логики и логические выражения

Источники:

- https://future-step.ru/tutor/task-2-1/
- https://future-step.ru/tutor/task-15-1/

## К каким заданиям относится

Задания 2 и 15.

## Цель занятия

Научиться вычислять логические выражения, строить таблицы истинности и переводить логические операции в Python.

## Базовые операции

| Операция | Смысл | Python |
|---|---|---|
| `НЕ A` | инверсия | `not A` |
| `A И B` | конъюнкция | `A and B` |
| `A ИЛИ B` | дизъюнкция | `A or B` |
| `A -> B` | импликация | `not A or B`, также `A <= B` для bool |
| `A == B` | эквивалентность | `A == B` |

## Приоритет операций

В логике:

1. скобки;
2. отрицание;
3. конъюнкция;
4. дизъюнкция;
5. импликация;
6. эквивалентность.

В Python важно помнить: сравнения вроде `<=` и `==` имеют свой приоритет. Для ЕГЭ безопаснее всегда ставить скобки вокруг импликаций и эквивалентностей.

## Таблица истинности

Для `n` переменных в таблице будет `2 ** n` строк.

```python
from itertools import product

for x, y, z in product([0, 1], repeat=3):
    f = (x and not y) or z
    print(x, y, z, int(f))
```

## Алгоритм для задания 2

1. Выписать переменные.
2. Переписать выражение на Python.
3. Перебрать все наборы 0 и 1.
4. Оставить строки, которые подходят по условию.
5. Сопоставить порядок столбцов с условием задачи.

## Алгоритм для задания 15

1. Переписать условие как логическое выражение.
2. Определить диапазон перебора переменной `A`.
3. Проверить, при каких `A` выражение истинно для всех нужных `x`.
4. Выбрать минимальное или максимальное `A` по условию.

## Частые ошибки

- Путать импликацию с обычным `if`.
- Записывать `A -> B` как `A or B`.
- Не ставить скобки.
- Использовать `=` вместо `==`.
